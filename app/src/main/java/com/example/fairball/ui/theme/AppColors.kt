package com.example.fairball.ui.theme

import androidx.compose.ui.graphics.Color

/** Colori usati letteralmente in più punti dell'app, centralizzati per evitare valori duplicati. */
object AppColors {
    val PendingApprovalAmber = Color(0xFFFFA000)
}
